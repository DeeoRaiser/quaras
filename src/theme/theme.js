import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: { main: "#dd1111" },
        secondary: { main: "#f947b0" },
        success: { main: "#4caf50" },
        error: { main: "#f44336" },
        warning: { main: "#ff9800" },
    },
});

export default theme;   